import fs from "node:fs";
import path from "node:path";
const directory=path.resolve("src/content/posts");
const files=fs.readdirSync(directory).filter(f=>f.endsWith(".json"));
const slugs=new Set(),titles=new Set(),errors=[];
for(const file of files){try{const post=JSON.parse(fs.readFileSync(path.join(directory,file),"utf8"));for(const key of ["slug","title","summary","category","publishedAt","paragraphs","references"])if(!post[key])errors.push(`${file}: ${key} is required`);if(!["finance","engineering"].includes(post.category))errors.push(`${file}: invalid category`);if(slugs.has(post.slug))errors.push(`${file}: duplicate slug`);if(titles.has(post.title))errors.push(`${file}: duplicate title`);if(!Array.isArray(post.paragraphs)||post.paragraphs.length<8||post.paragraphs.length>12)errors.push(`${file}: 8-12 paragraphs required`);const body=Array.isArray(post.paragraphs)?post.paragraphs.join(""):"";if(body.length<1000||body.length>1600)errors.push(`${file}: body must be 1000-1600 characters, excluding references`);if(!Array.isArray(post.references)||post.references.length<2)errors.push(`${file}: at least 2 references required`);else for(const [index,reference] of post.references.entries()){if(!reference?.title||!reference?.publisher||!reference?.url)errors.push(`${file}: reference ${index+1} requires title, publisher, and url`);else{try{const url=new URL(reference.url);if(url.protocol!=="https:")errors.push(`${file}: reference ${index+1} must use https`)}catch{errors.push(`${file}: reference ${index+1} has an invalid url`)}}}if(post.sections)errors.push(`${file}: section headings are not allowed`);slugs.add(post.slug);titles.add(post.title)}catch(error){errors.push(`${file}: ${error.message}`)}}
if(errors.length){console.error(errors.join("\n"));process.exit(1)}
console.log(`Validated ${files.length} posts.`);
