import React from "react";

const Footer = () => {
  return (
    <footer className="footer">
      <small>© {new Date().getFullYear()} tanakashoi</small>
    </footer>
  );
};

export default Footer;
